[project]
name = "server"
version = "0.1.0"
description = "Searches ahadith using the power of LLM"
requires-python = ">=3.14"
dependencies = [
    "groq>=1.1.2",
    "ollama>=0.6.1",
    "python-dotenv",
    "aiofiles",
    "qdrant-client",
    "google-genai"
]
